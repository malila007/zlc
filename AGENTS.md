# AGENTS.md

Codex entrypoint. **Read `PROJECT.md` first** — it is canonical and owns the reading order, the docs-on-change table, and the pre-release E2E gate.

General workflow, roles, skills, and coding rules live in the machine-wide `~/.codex/AGENTS.md`.

Scope: `chat-service`, `floating-chat`, and the `backoffice-frontend` chat module only.
