# .notes/

Short-term AI memory for this project. Long-term knowledge lives in `PROJECT.md` and the global agent guides — not here.

| Path | What |
|------|------|
| `sessions/YYYY-MM-DD-<topic>.md` | Per-session summary. Never overwrite — new file each session. |
| `<feature>.md` | One file per active feature — decisions, status, open items. |
| `deploy-YYYY-MM-DD.md` | Deploy guides when needed. |

Keep everything terse. Promote durable facts to `PROJECT.md`.

Issues and risks are tracked in **Jira** (project/space `ZLC`, named **Chat**, on `nutcom3.atlassian.net`) via the Atlassian MCP — not here. Use only the Chat Jira space for this repo. Ticket format lives in `PROJECT.md`.
